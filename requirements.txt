ultralytics>=8.3.0
opencv-python>=4.9.0
numpy>=1.26.0
scipy>=1.12.0
motmetrics>=1.4.0
pytest>=8.0.0

# NOTE: motmetrics 1.4.0's own iou_matrix() uses np.asfarray(), removed in
# NumPy 2.0. We avoid that path entirely in eval_mot17.py by computing IOU
# ourselves (see src/tracking/matching.py), so this works fine with numpy>=2
# and normal opencv-python -- no need to downgrade numpy.
